package kiwi.ingenuity.netbeans.plugin.aicoder.process.events;

/**
 * Common supertype for all AI backend events. Concrete events implement this
 * directly. Each AI backend produces instances of the shared event types or its
 * own subtypes.
 */
public interface AiProcessEvent {
}
