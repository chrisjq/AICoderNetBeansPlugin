package kiwi.ingenuity.netbeans.plugin.aicoder.ai.impl.opencode.ui;

/**
 * Info bar callbacks for OpenCode. Mirrors OllamaInfoBarListener and
 * ClaudeInfoBarListener. Model changes keep their existing
 * addModelChangeListener(ActionListener) route — adding a typed method here
 * would give two paths to the same behaviour.
 */
public interface OpenCodeInfoBarListener {

}
