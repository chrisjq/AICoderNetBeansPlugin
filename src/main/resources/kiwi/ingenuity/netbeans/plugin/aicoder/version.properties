# Maven-filtered at build time - see the resource <filtering>true</filtering> block
# in pom.xml. These are the pom's own values, so nothing here should ever be
# edited to a literal: change pom.xml and the build carries it through.
version=${project.version}
homepage=${project.url}
